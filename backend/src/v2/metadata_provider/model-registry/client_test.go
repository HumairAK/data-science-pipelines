package mlflow

import (
	"testing"
)

// TestCreateRunFunction tests the createRun function directly
func TestCreateRunFunction(t *testing.T) {
	//// Create a test client with a custom createRun function
	//client := &testClient{}
	//
	//// Test parameters
	//runName := "test-run"
	//description := "test description"
	//experimentID := "test-experiment-id"
	//
	//// Call the function
	//run, err := client.createRun(runName, nil, description, experimentID)
	//
	//// Assertions
	//assert.NoError(t, err)
	//assert.NotNil(t, run)
	//assert.Equal(t, "test-run-id", *run.Id)
	//assert.Equal(t, runName, *run.Name)
	//assert.Equal(t, description, *run.Description)
	//assert.Equal(t, experimentID, run.ExperimentId)
}
